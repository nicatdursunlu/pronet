import React from 'react';
import styled from 'styled-components';

import { Aside } from '../../commons';

export const InvestmentPlansPage = () => {
  return(
    <Container>
      <Aside />
    </Container>
  );
};

const Container = styled.div({

});